<!--
Template for convert-mockup.js
Placeholders:
  Timeline - PascalCase component name (e.g. ChatBubble)
  timeline - original mockup tag (e.g. chat-bubble)
  harden-timeline - id value or null
  {
  "tag": "timeline",
  "id": "harden-timeline",
  "attrs": {
    "id": "harden-timeline",
    "source": "index.yaml",
    "behavior": " Click on a point = preview that version, Each point = a Harden with label + message + date"
  }
} - JSON object of attributes
-->
<script module lang="ts">
  /**
   * Component: Timeline
   * Type definitions for external usage
   */
  export type TimelineProps = {
    children?: import('svelte').Snippet;
    [key: string]: any;
  };

  export const mockup = {
  "tag": "timeline",
  "id": "harden-timeline",
  "attrs": {
    "id": "harden-timeline",
    "source": "index.yaml",
    "behavior": " Click on a point = preview that version, Each point = a Harden with label + message + date"
  }
};
</script>

<script lang="ts">
  // '...rest' captures any other attributes (id, class, etc.)
  let { children, ...rest }: TimelineProps = $props();
</script>

<section class="timeline" {...rest}>
    {@render children?.()}
</section>

<style lang="postcss">
  .timeline {
    /* Component styles */
  }
</style>
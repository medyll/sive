<!--
Template for convert-mockup.js
Placeholders:
  DiffView - PascalCase component name (e.g. ChatBubble)
  diff-view - original mockup tag (e.g. chat-bubble)
  suggestions-diff - id value or null
  {
  "tag": "diff-view",
  "id": "suggestions-diff",
  "attrs": {
    "id": "suggestions-diff",
    "left": "original text",
    "right": "AI proposal",
    "behavior": " Each change = a proposal,Each proposal can be independently accepted or rejected, Click on a change = show details in the suggestion actions below"
  }
} - JSON object of attributes
-->
<script module lang="ts">
  /**
   * Component: DiffView
   * Type definitions for external usage
   */
  export type DiffViewProps = {
    children?: import('svelte').Snippet;
    [key: string]: any;
  };

  export const mockup = {
  "tag": "diff-view",
  "id": "suggestions-diff",
  "attrs": {
    "id": "suggestions-diff",
    "left": "original text",
    "right": "AI proposal",
    "behavior": " Each change = a proposal,Each proposal can be independently accepted or rejected, Click on a change = show details in the suggestion actions below"
  }
};
</script>

<script lang="ts">
  // '...rest' captures any other attributes (id, class, etc.)
  let { children, ...rest }: DiffViewProps = $props();
</script>

<section class="diff-view" {...rest}>
    {@render children?.()}
</section>

<style lang="postcss">
  .diff-view {
    /* Component styles */
  }
</style>
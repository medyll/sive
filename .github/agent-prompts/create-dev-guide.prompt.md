Create a concise `DEV.md` for this repo.

Inputs expected:
- Repo root path (default root)
- Primary package manager (pnpm or npm)

Output:
- A short `DEV.md` with install/run/test/db commands, env var list, auth warnings, and key file links.

Usage: run this prompt when onboarding new contributors or when changing scripts.
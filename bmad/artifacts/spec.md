# Product Specification (SPEC)

Merged on: 2026-03-14
Source: `bmad/artifacts/spec.draft.md`

This file is the canonical specification created from the draft. It describes the guest-mode implementation, API endpoints, testing and rollout plans.

## Overview

This document specifies the implementation details for making authentication optional (guest mode) while preserving security, data ownership, and the ability to convert guest data to authenticated accounts. It targets the minimal safe rollout (cookie-only guest id) as the initial implementation to avoid DB migrations.

## Requirements

- R1: When a request has no authenticated session, create/ensure a `guest_id` cookie (stable UUID) and set `event.locals.user` to `null`.
- R2: Non-sensitive read-only routes must work for guests; write or sensitive actions must require authentication and return `401/403` for guests.
- R3: Provide an endpoint to convert/merge guest data into a new or existing user account using verification (email/token). Conversion should be transactional.
- R4: Feature flag `ALLOW_GUESTS` to enable/disable guest behavior at runtime.

## API & Server Behavior

- Hook: `src/hooks.server.ts` — call `auth.api.getSession` then set `event.locals.guestId` cookie when needed (Secure, HttpOnly, SameSite=Lax).
- POST `/api/guest/convert` — validate guest cookie, create/link account, trigger ownership transfer or mark for background reconciliation.
- GET `/api/guest/status` — returns `{ guestId, canConvert }`.

## Testing

- Unit tests for hooks and route guards.
- E2E Playwright tests for guest flow, conversion, and negative cases.

## Rollout

- Default `ALLOW_GUESTS=false`. Canary with metrics monitoring, then global enable if safe.

---

File generated by bmad agent from draft; original draft remains at `bmad/artifacts/spec.draft.md`.
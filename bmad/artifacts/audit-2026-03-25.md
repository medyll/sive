# Audit Report — 2026-03-25

## Summary
- **Unit tests:** 80 files, 795 passed, 0 failures ✅
- **TypeScript errors:** 30 pre-existing (reduced from ~35 before this session)

## Errors Fixed This Session
- `commandRegistry.ts` — all `dispatchEvent` returns `boolean`, fixed with `void` operator (11 instances)
- `commandRegistry.ts` — `'plugin'` not in `CommandCategory` union → added
- `app.d.ts` — `isGuest` and `preferences` missing from `Locals` → added
- `presence-store.spec.ts` — `store` declared as `| null`, fixed with definite assignment; `never` narrowing in subscribe test fixed with object wrapper
- `data-loader.spec.ts` — optional chain `?.` on `tensionCurve`, `objects`, `vehicles`, `transitions`
- `page.server.spec.ts` — load result cast to `Record<string, any>` where needed
- `profile/page.server.spec.ts` — same cast pattern

## Remaining Pre-existing Errors (30)

### High priority
| File | Error | Notes |
|------|-------|-------|
| `src/lib/server/auth.ts:3` | `BetterAuth` → `betterAuth` rename in better-auth API | Import type changed upstream |
| `src/lib/server/auth.ts:44`, `db/index.ts:31`, `+page.server.ts:87`, `auth/+server.ts:22` | `.message` on `{}` | Error catch clauses need `(e as Error).message` |
| `src/routes/api/export/docx/+server.ts:14`, `epub/+server.ts:14` | `Uint8Array<ArrayBufferLike>` not assignable to `BodyInit` | TypeScript 5.6 stricter `Uint8Array` generics — needs `new Response(Buffer.from(buf))` |
| `src/lib/notificationStore.spec.ts` | Missing `userId` on Notification fixture | Add `userId: 'u1'` |

### Low priority (test-only)
| File | Error | Notes |
|------|-------|-------|
| `cursor-sync.spec.ts:206` | `getDocumentPresence` missing from cursor store type | Property added after spec was written |
| `PresenceList.svelte.spec.ts:4`, `RemoteCursor.svelte.spec.ts:4` | Named imports from `.svelte` files | Should import from separate `.ts` type files |
| `SummaryPanel.svelte.spec.ts:41` | Empty tuple access | Test fixture issue |
| `data-loader.spec.ts` | Many `possibly undefined` | Add `?.` on remaining accesses |
| `ws.spec.ts:162` | `.type` missing | WS message type narrowing |
| `dashboard/+page.server.ts:36` | Implicit `any` param | Add `: unknown` |

## Recommendations
1. **Fix auth.ts** (`BetterAuth` import) — blocks type safety for auth layer
2. **Fix `.message` on `{}`** — pattern: `(e as Error).message ?? String(e)`
3. **Fix Uint8Array export** — use `Buffer.from()` wrapper
4. **Fix notificationStore.spec.ts** — trivial, add `userId` field
5. **Sprint 63** — can proceed; none of these errors block runtime functionality

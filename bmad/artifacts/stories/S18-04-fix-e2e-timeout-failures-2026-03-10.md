# S18-04 — Fix E2E timeout failures (2026-03-10)

Summary:
- Playwright E2E run failed with many timeouts (locator.click, page.waitForURL, expect(...).toBeVisible).
- Several tests produced Test timeout (30000ms) or smaller expect timeouts (5000ms).
- Web server warnings present during the run: "better-auth init failed; require is not defined" and "better-sqlite3 failed to load, running with a mocked DB".
- Detailed error contexts were written to test-results/* (see list below).

Top failing areas (examples):
- app-layout (many timeouts & missing elements)
- ai-integration (locator click timeouts)
- coherence-tab, style-tab, suggestions-tab, sharing-flow, document-persistence

Key error excerpts:
- "Test timeout of 30000ms exceeded. Error: locator.click: Test timeout of 30000ms exceeded."
- "Error: expect(locator).toBeVisible() failed — element(s) not found"
- "strict mode violation: getByRole('button', { name: 'Suggestions' }) resolved to 2 elements"
- "[WebServer] better-auth init failed; using auth stub for dev: require is not defined"

Reproduction:
1. From project root run: npm run test:e2e
2. Inspect test output and artifacts in test-results/ and the Playwright output saved at:
   C:\Users\Mydde\AppData\Local\Temp\copilot-tool-output-1773205824519-j6rupp.txt
3. Open per-test error contexts under test-results/<test-folder>/error-context.md

Suggested next steps:
1. Reproduce locally with Playwright --debug to capture screenshots and traces.
2. Verify the app server is starting and responsive before test runs (add readiness probe or use Playwright's web-server option).
3. Investigate and fix the "require is not defined" warnings—migrate CJS require usages or provide compatibility in the test environment.
4. Ensure better-sqlite3 native binary is available in CI or provide a stable mock for tests.
5. Harden flaky selectors/waits in tests and consider small timeout increases where appropriate.
6. Re-run E2E suite and iterate until green.

Artifacts and references:
- Playwright raw output (temp): C:\Users\Mydde\AppData\Local\Temp\copilot-tool-output-1773205824519-j6rupp.txt
- Per-test error-contexts: test-results/*/error-context.md
- Integration test artifact: bmad/artifacts/sprints/S18-02-final-integration-tests-2026-03-10.md

Owner: Unassigned
Status: pending

Notes:
- This story was auto-generated by bmad-master next --auto after E2E failures were detected.

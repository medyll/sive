# Story S17-05 — Unit tests — OAuth + shared docs

**Epic:** QA
**Sprint:** Sprint 17
**Points:** 3
**Priority:** Must

## User Story

As a **developer**, I want unit tests for the new Sprint 17 server logic, so that regressions are caught automatically.

## Acceptance Criteria

```gherkin
Given the auth page server load function
When GITHUB_CLIENT_ID is set
Then hasGithub is true in the returned data

Given the auth page server load function
When GITHUB_CLIENT_ID is absent
Then hasGithub is false

Given the profile updateName action
When called with a valid name
Then the user's name is updated in the DB

Given a user has share records
When the app page loads
Then their documents include shared docs with correct role annotation
```

## Definition of Done

- [ ] 8+ new unit tests passing
- [ ] PR reviewed and merged
